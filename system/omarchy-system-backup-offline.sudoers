# Let the desktop user's detached units back up or verify the small privileged
# system-state repository. No arguments, alternate units, or shell are allowed.
crab ALL=(root) NOPASSWD: /usr/bin/systemctl start --wait omarchy-system-backup-offline.service, /usr/bin/systemctl start --wait omarchy-system-backup-offline-verify.service
